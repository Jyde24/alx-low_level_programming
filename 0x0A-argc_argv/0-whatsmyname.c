#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
  * main - This program that prints its name, followed by a new line.
  * @argc : Number of argument accepted.
  * @argv : The argument's array.
  * Return: returns 0
  */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
